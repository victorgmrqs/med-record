post:
  summary: "Create a new patient"
  tags:
    - Patients
  security:
    - bearerAuth: []
  requestBody:
    required: true
    content:
      application/json:
        schema:
          $ref: "./components.yaml#/schemas/CreatePatientBody"
  responses:
    $ref: "./_post.yaml" # arquivo que contém os status codes de resposta (201, 400, etc.)

get:
  summary: "Get all patients"
  tags:
    - Patients
  security:
    - bearerAuth: []
  responses:
    $ref: "./_getAll.yaml"
