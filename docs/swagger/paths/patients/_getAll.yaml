200:
  description: "Array of patients"
  content:
    application/json:
      schema:
        $ref: "./components.yaml#/schemas/PatientsListResponse"
401:
  content:
    application/json:
      schema:
        $ref: "../../components.yaml#/schemas/UnauthorizedError"
500:
  description: Internal server error
  content:
    application/json:
      schema:
        $ref: "../../components.yaml#/schemas/InternalServerError"
